import React from 'react';
import './Loader.css'; // створіть CSS файл для стилізації лоадера

const Loader = () => (
  <div className="loader">
    <div className="spinner"></div>
  </div>
);

export default Loader;